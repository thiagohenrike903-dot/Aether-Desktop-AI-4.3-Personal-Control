# Aether Desktop — lean Python core
# Python 3.10+ required. Computer-vision packages are optional and live in
# requirements-vision.txt so first launch stays fast and reliable.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic>=2.10.0

# HTTP / async
httpx==0.27.2
httpcore>=1.0.5,<1.1
anyio>=4.4,<5
aiofiles==24.1.0
python-multipart==0.0.10

# OS automation
psutil==6.0.0
pywin32==311 ; sys_platform == "win32"
pycaw==20240210 ; sys_platform == "win32"
comtypes==1.2.1 ; sys_platform == "win32"
screen-brightness-control==0.18.0

# Screenshot (used by VLM)
mss==10.0.0
Pillow>=11.0.0

# Speech synthesis. Speech recognition uses Chromium's native speech service.
elevenlabs==1.8.0

# Web search & content extraction
duckduckgo-search>=7.5.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Email
google-api-python-client>=2.120.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# Calendar
google-api-python-client>=2.120.0

# PDF processing
PyMuPDF>=1.24.0

# Crypto
cryptography>=42.0.0

# Utilities
python-dotenv==1.0.1
pytest>=8.0.0
